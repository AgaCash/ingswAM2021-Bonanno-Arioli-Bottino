package cards;
/*
class LeaderCardTest {

    public void test() {
        try {
            Gson gson = new Gson();
            Reader reader = Files.newBufferedReader(Paths.get("src/main/resources/leaderCards.json"));
            List<WhiteConverter> prova = Arrays.asList(gson.fromJson(reader, WhiteConverter[].class));


        } catch (Exception ex) {
            ex.printStackTrace();
        }
    }
}*/